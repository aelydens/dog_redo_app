<%= form_for([@dog, @toy]) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit "Create New Toy for #{@dog.name}" %>
<% end %>

<hr>
Toys
<hr>

<% @dog.toys.each do |toy| %>
<%= toy.name %>
<% end %>

<hr>

Make an Appointment for <%= @dog.name %> Below:

<%= form_for([@dog, @appointment]) do |f| %>
  <%= f.label :time %>
  <%= f.time_field :time %>
  <%= f.label :vet_id, "Vet Name" %>
  <%= f.collection_select(:vet_id, Vet.all, :id, :name) %>
  <%= f.submit %>
<% end %>

<hr>
Appointments

<% @dog.appointments.each do |appointment| %>
  <%= appointment.time %>
  <%= appointment.vet.name %>
<% end %>

<hr>
<%= link_to "Back to Owner", user_path(@dog.user) %>
